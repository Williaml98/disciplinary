# Base URL for the CaseFlow API. Not yet consumed anywhere in the app —
# CaseFlow-BE doesn't exist yet, so the UI still runs entirely on mock data
# (src/app/components/mockData.tsx). Wire this up once a real backend exists.
VITE_API_BASE_URL=http://localhost:3000/api
