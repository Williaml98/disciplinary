# Base URL for the CaseFlow API (CaseFlow-BE). Defaults to localhost:8080 in
# src/config.ts if this isn't set, so a .env file is only needed to override it.
VITE_API_BASE_URL=http://localhost:8080/api
